# Higher-order artifact audit

- **PASS** — reachable Cognitive Workflow Studio created an `ArtifactRef` from a real Move and proposed a bounded semantic `ArtifactPatch`.
- **PASS** — the candidate ran in an isolated snapshot before review; the visible review exposed selectable hunks and test status.
- **PASS** — accepting selected hunks created a new version and a patch receipt while preserving the original version reference.
- **PASS** — stale fingerprints, protected registry paths, duplicate paths, recursion depth, and operation count fail before mutation.
- **PASS** — the real CompanionChat command “open cognitive workflow studio” reached the same visible review surface with director animation.
- **PASS** — extension parity uses a preserved, explicit web handoff for graph-heavy edits.

Evidence: `workflow-results.json`, `01-isolated-selective-patch.png`, `02-companion-visible-workflow.png`.
