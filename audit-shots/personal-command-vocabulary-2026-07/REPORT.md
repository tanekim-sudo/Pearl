# Personal command vocabulary audit

- **PASS** — reachable UI previewed and confirmed a workspace-scoped personal command before persistence.
- **PASS** — exact and parameterized aliases resolve deterministically before open-ended planning while retaining the original utterance.
- **PASS** — quoted/literal use does not execute; reserved confirmation words, collisions, and recursion cycles are rejected.
- **PASS** — definitions retain scope, version, provenance, inherited risk, status, and expiry; users can test, disable, enable, and forget them.
- **PASS** — conflict-aware merges choose the newest definition version and extension storage uses the same canonical definition validator.
- **PASS** — extension teaching requires scoped confirmation and never silently publishes vocabulary in a package.

Evidence: `workflow-results.json`, `01-scoped-literal-safe-vocabulary.png`.
